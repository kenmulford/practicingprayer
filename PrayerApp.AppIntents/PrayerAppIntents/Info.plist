<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>EXAppExtensionAttributes</key>
	<dict>
		<key>EXExtensionPointIdentifier</key>
		<string>com.apple.appintents-extension</string>
	</dict>
	<!--
	  NSExtension is required IN ADDITION to EXAppExtensionAttributes because
	  this .appex is embedded into a .NET MAUI host via <AdditionalAppExtensions>,
	  which lands in PlugIns/ (legacy PlugInKit location). iOS's installcoordinationd
	  reads NSExtension to populate placeholder attributes during install — without
	  it, install fails with IXErrorDomain Code=17 "extensionDictionary must be set".
	  Both keys point at com.apple.appintents-extension so registration works on
	  both code paths. Verified on iPhone 17 sim iOS 26.4 (commit be60bce).
	-->
	<key>NSExtension</key>
	<dict>
		<key>NSExtensionPointIdentifier</key>
		<string>com.apple.appintents-extension</string>
	</dict>
</dict>
</plist>
