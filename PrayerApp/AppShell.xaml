<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="PrayerApp.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:PrayerApp"
    xmlns:views="clr-namespace:PrayerApp.Views"
    xmlns:tagViews="clr-namespace:PrayerApp.Views.Tags"
    xmlns:settingsViews="clr-namespace:PrayerApp.Views.Settings"
    Title="PrayerApp">

    <TabBar>

        <ShellContent
            Title="Home"
            Icon="house_solid_full.png"
            ContentTemplate="{DataTemplate views:MainPage}"
            Route="MainPage" />

        <ShellContent
            Title="Prayer Cards"
            Icon="list_solid_full.png"
            ContentTemplate="{DataTemplate views:PrayerCard.PrayerCardsPage}"
            Route="CardsPage" />

        <ShellContent
            Title="Prayers"
            Icon="hands_praying_solid_full.png"
            ContentTemplate="{DataTemplate views:Prayer.PrayerListPage}"
            Route="PrayersPage" />

        <ShellContent
            Title="Tags"
            Icon="tag_solid_full.png"
            ContentTemplate="{DataTemplate tagViews:TagsPage}"
            Route="TagsPage" />

        <ShellContent
            Title="Settings"
            Icon="gear_solid_full.png"
            ContentTemplate="{DataTemplate settingsViews:SettingsHubPage}"
            Route="Settings" />

    </TabBar>
</Shell>
