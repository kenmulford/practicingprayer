namespace PrayerApp.Helpers
{
    /// <summary>
    /// All user-visible strings for the Boxes/Folders feature.
    /// Changing these values is a single-file rename when the external naming is finalized.
    /// Internal model name (CardBox) is unaffected.
    /// </summary>
    public static class BoxStrings
    {
        public const string Word = "Folder";
        public const string Plural = "Folders";
        public const string Unorganized = "Unorganized";
    }
}
