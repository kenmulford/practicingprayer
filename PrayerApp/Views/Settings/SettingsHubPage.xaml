<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             x:Class="PrayerApp.Views.Settings.SettingsHubPage"
             Title="Settings"
             SafeAreaEdges="Container">

    <ScrollView>
        <VerticalStackLayout Padding="20,16" Spacing="16">

            <Border Style="{StaticResource PrayerCardBorder}" Padding="0">
                <VerticalStackLayout Spacing="0">

                    <!-- App Settings -->
                    <Grid ColumnDefinitions="Auto,*,Auto" Padding="16,14"
                          AutomationId="Settings_Row_AppSettings"
                          SemanticProperties.Hint="Double tap to open">
                        <Grid.GestureRecognizers>
                            <TapGestureRecognizer Tapped="OnAppSettingsTapped" />
                        </Grid.GestureRecognizers>
                        <Image Grid.Column="0"
                               Source="gear_solid_full.png"
                               HeightRequest="22" WidthRequest="22"
                               VerticalOptions="Center"
                               Margin="0,0,14,0"
                               AutomationProperties.IsInAccessibleTree="False">
                            <Image.Behaviors>
                                <toolkit:IconTintColorBehavior TintColor="{AppThemeBinding
                                    Light={StaticResource Gray600},
                                    Dark={StaticResource TextSecondaryDark}}" />
                            </Image.Behaviors>
                        </Image>
                        <VerticalStackLayout Grid.Column="1" Spacing="2">
                            <Label Text="App Settings"
                                   FontSize="16"
                                   FontAttributes="Bold"
                                   TextColor="{AppThemeBinding Light={StaticResource Gray900}, Dark={StaticResource TextPrimaryDark}}" />
                            <Label Text="Notifications &amp; reminders"
                                   FontSize="13"
                                   TextColor="{AppThemeBinding Light={StaticResource Gray500}, Dark={StaticResource TextSecondaryDark}}" />
                        </VerticalStackLayout>
                        <Label Grid.Column="2"
                               Text="›"
                               FontSize="20"
                               VerticalOptions="Center"
                               TextColor="{AppThemeBinding Light={StaticResource Gray400}, Dark={StaticResource TextMutedDark}}"
                               AutomationProperties.IsInAccessibleTree="False" />
                    </Grid>

                    <BoxView Style="{StaticResource DividerLine}" />

                    <!-- Manage Collections -->
                    <Grid ColumnDefinitions="Auto,*,Auto" Padding="16,14"
                          AutomationId="Settings_Row_Collections"
                          SemanticProperties.Hint="Double tap to open">
                        <Grid.GestureRecognizers>
                            <TapGestureRecognizer Tapped="OnCollectionsTapped" />
                        </Grid.GestureRecognizers>
                        <Image Grid.Column="0"
                               Source="list_solid_full.png"
                               HeightRequest="22" WidthRequest="22"
                               VerticalOptions="Center"
                               Margin="0,0,14,0"
                               AutomationProperties.IsInAccessibleTree="False">
                            <Image.Behaviors>
                                <toolkit:IconTintColorBehavior TintColor="{AppThemeBinding
                                    Light={StaticResource Gray600},
                                    Dark={StaticResource TextSecondaryDark}}" />
                            </Image.Behaviors>
                        </Image>
                        <VerticalStackLayout Grid.Column="1" Spacing="2">
                            <Label Text="Manage Collections"
                                   FontSize="16"
                                   FontAttributes="Bold"
                                   TextColor="{AppThemeBinding Light={StaticResource Gray900}, Dark={StaticResource TextPrimaryDark}}" />
                            <Label Text="Organize your prayer cards into groups"
                                   FontSize="13"
                                   TextColor="{AppThemeBinding Light={StaticResource Gray500}, Dark={StaticResource TextSecondaryDark}}" />
                        </VerticalStackLayout>
                        <Label Grid.Column="2"
                               Text="›"
                               FontSize="20"
                               VerticalOptions="Center"
                               TextColor="{AppThemeBinding Light={StaticResource Gray400}, Dark={StaticResource TextMutedDark}}"
                               AutomationProperties.IsInAccessibleTree="False" />
                    </Grid>

                    <BoxView Style="{StaticResource DividerLine}" />

                    <!-- Backup & Restore -->
                    <Grid ColumnDefinitions="Auto,*,Auto" Padding="16,14"
                          AutomationId="Settings_Row_Backup"
                          SemanticProperties.Hint="Double tap to open">
                        <Grid.GestureRecognizers>
                            <TapGestureRecognizer Tapped="OnBackupTapped" />
                        </Grid.GestureRecognizers>
                        <Image Grid.Column="0"
                               Source="cloud_arrow_up_solid_full.png"
                               HeightRequest="22" WidthRequest="22"
                               VerticalOptions="Center"
                               Margin="0,0,14,0"
                               AutomationProperties.IsInAccessibleTree="False">
                            <Image.Behaviors>
                                <toolkit:IconTintColorBehavior TintColor="{AppThemeBinding
                                    Light={StaticResource Gray600},
                                    Dark={StaticResource TextSecondaryDark}}" />
                            </Image.Behaviors>
                        </Image>
                        <VerticalStackLayout Grid.Column="1" Spacing="2">
                            <Label Text="Backup &amp; Restore"
                                   FontSize="16"
                                   FontAttributes="Bold"
                                   TextColor="{AppThemeBinding Light={StaticResource Gray900}, Dark={StaticResource TextPrimaryDark}}" />
                            <Label Text="Export or restore your data"
                                   FontSize="13"
                                   TextColor="{AppThemeBinding Light={StaticResource Gray500}, Dark={StaticResource TextSecondaryDark}}" />
                        </VerticalStackLayout>
                        <Label Grid.Column="2"
                               Text="›"
                               FontSize="20"
                               VerticalOptions="Center"
                               TextColor="{AppThemeBinding Light={StaticResource Gray400}, Dark={StaticResource TextMutedDark}}"
                               AutomationProperties.IsInAccessibleTree="False" />
                    </Grid>

                    <BoxView Style="{StaticResource DividerLine}" />

                    <!-- About -->
                    <Grid ColumnDefinitions="Auto,*,Auto" Padding="16,14"
                          AutomationId="Settings_Row_About"
                          SemanticProperties.Hint="Double tap to open">
                        <Grid.GestureRecognizers>
                            <TapGestureRecognizer Tapped="OnAboutTapped" />
                        </Grid.GestureRecognizers>
                        <Image Grid.Column="0"
                               Source="circle_info_solid_full.png"
                               HeightRequest="22" WidthRequest="22"
                               VerticalOptions="Center"
                               Margin="0,0,14,0"
                               AutomationProperties.IsInAccessibleTree="False">
                            <Image.Behaviors>
                                <toolkit:IconTintColorBehavior TintColor="{AppThemeBinding
                                    Light={StaticResource Gray600},
                                    Dark={StaticResource TextSecondaryDark}}" />
                            </Image.Behaviors>
                        </Image>
                        <VerticalStackLayout Grid.Column="1" Spacing="2">
                            <Label Text="About"
                                   FontSize="16"
                                   FontAttributes="Bold"
                                   TextColor="{AppThemeBinding Light={StaticResource Gray900}, Dark={StaticResource TextPrimaryDark}}" />
                            <Label Text="Privacy, version &amp; links"
                                   FontSize="13"
                                   TextColor="{AppThemeBinding Light={StaticResource Gray500}, Dark={StaticResource TextSecondaryDark}}" />
                        </VerticalStackLayout>
                        <Label Grid.Column="2"
                               Text="›"
                               FontSize="20"
                               VerticalOptions="Center"
                               TextColor="{AppThemeBinding Light={StaticResource Gray400}, Dark={StaticResource TextMutedDark}}"
                               AutomationProperties.IsInAccessibleTree="False" />
                    </Grid>

                    <BoxView Style="{StaticResource DividerLine}" />

                    <!-- Help -->
                    <Grid ColumnDefinitions="Auto,*,Auto" Padding="16,14"
                          AutomationId="Settings_Row_Help"
                          SemanticProperties.Hint="Double tap to open">
                        <Grid.GestureRecognizers>
                            <TapGestureRecognizer Tapped="OnHelpTapped" />
                        </Grid.GestureRecognizers>
                        <Image Grid.Column="0"
                               Source="circle_question_solid_full.png"
                               HeightRequest="22" WidthRequest="22"
                               VerticalOptions="Center"
                               Margin="0,0,14,0"
                               AutomationProperties.IsInAccessibleTree="False">
                            <Image.Behaviors>
                                <toolkit:IconTintColorBehavior TintColor="{AppThemeBinding
                                    Light={StaticResource Gray600},
                                    Dark={StaticResource TextSecondaryDark}}" />
                            </Image.Behaviors>
                        </Image>
                        <VerticalStackLayout Grid.Column="1" Spacing="2">
                            <Label Text="Help"
                                   FontSize="16"
                                   FontAttributes="Bold"
                                   TextColor="{AppThemeBinding Light={StaticResource Gray900}, Dark={StaticResource TextPrimaryDark}}" />
                            <Label Text="Tips &amp; frequently asked questions"
                                   FontSize="13"
                                   TextColor="{AppThemeBinding Light={StaticResource Gray500}, Dark={StaticResource TextSecondaryDark}}" />
                        </VerticalStackLayout>
                        <Label Grid.Column="2"
                               Text="›"
                               FontSize="20"
                               VerticalOptions="Center"
                               TextColor="{AppThemeBinding Light={StaticResource Gray400}, Dark={StaticResource TextMutedDark}}"
                               AutomationProperties.IsInAccessibleTree="False" />
                    </Grid>

                </VerticalStackLayout>
            </Border>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
