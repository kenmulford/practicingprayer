<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:PrayerApp.ViewModels"
             x:Class="PrayerApp.Views.Prayer.PrayerListPage"
             x:DataType="viewModels:PrayerListViewModel"
             Title="Prayers">
    <ContentPage.BindingContext>
        <viewModels:PrayerListViewModel />
    </ContentPage.BindingContext>
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Add" Command="{Binding NewCommand}" />
    </ContentPage.ToolbarItems>
    <ScrollView>
        <VerticalStackLayout
        Padding="30,0"
        Spacing="25">
            <Label
            Text="Manage Prayers"
            Style="{StaticResource Headline}"
            SemanticProperties.HeadingLevel="Level1" />
            <CollectionView 
                x:Name="prayerCollection"
                ItemsSource="{Binding AllPrayers}"
                Margin="10"
                SelectionMode="None">
                <CollectionView.ItemsLayout>
                    <LinearItemsLayout Orientation="Vertical" ItemSpacing="10" />
                </CollectionView.ItemsLayout>

                <!-- Define the appearance of each item in the list -->
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="viewModels:PrayerDetailViewModel">
                        <Grid
                            BackgroundColor="Transparent"
                            Padding="5"
                            RowDefinitions="*,*"
                            ColumnDefinitions="*"
                            HorizontalOptions="Fill"
                            VerticalOptions="Fill">
                            <Grid.GestureRecognizers>
                                <TapGestureRecognizer
                                    Command="{Binding SelectPrayerCommand}" />
                            </Grid.GestureRecognizers>
                            <Label
                                FontSize="22"
                                Grid.Row="0"
                                Text="{Binding Title}" />
                            
                            
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>